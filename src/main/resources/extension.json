{
  "entrypoint": "${project.group}.${project.mainClass}",
  "name": "${project.name}",
  "version": "${project.version}",
  "dependencies": [
    "Kepi",
    "KStom",
    "Actions"
  ],
  "meta": {
    "downloadURL": "https://github.com/Project-Cepi/LuaecEditor/releases/download/latest/LuaeEngine-1.0.0-all.jar"
  }
}